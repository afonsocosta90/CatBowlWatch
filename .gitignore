# Model artifacts (large binaries — push to HF or release assets)
models/*.pt
models/*.onnx
models/*.engine

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Ultralytics training run artifacts
runs/

# C++ build
build/
*.o
*.a

# Env / secrets
.env

# macOS
.DS_Store

# Dataset — raw drops + generated splits. data/videos/ and data/data.yaml stay tracked.
data/raw/
data/images/train/
data/images/val/
data/images/test/
data/labels/train/
data/labels/val/
data/labels/test/

# Python test cache
.pytest_cache/

# Claude Code context files — local dev aid, not for the repo
CLAUDE.md
AGENTS.md
